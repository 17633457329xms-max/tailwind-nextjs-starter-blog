# 本地项目重启说明

项目目录：`D:\project\code2\temp_pastemd\paper\tailwind-nextjs-starter-blog`

本项目使用 Yarn 3。若 PowerShell 提示找不到 `yarn`，请始终使用 `corepack yarn`，不要改用全局安装的 Yarn。

## 一、正常重启（优先使用）

1. 打开 PowerShell。
2. 执行下面命令进入项目目录：

```powershell
Set-Location 'D:\project\code2\temp_pastemd\paper\tailwind-nextjs-starter-blog'
```

3. 如果当前终端正运行网站，按 `Ctrl + C` 停止服务。
4. 执行：

```powershell
corepack yarn start
```

5. 看到 `Ready` 和 `http://localhost:3000` 后，在浏览器打开：

```text
http://localhost:3000
```

此方式会让终端持续占用；不要关闭该终端，否则本地网站会停止。

## 二、出现 `Cannot find module './xxx.js'`、页面报错或修改未生效时

这是 Next.js 的 `.next` 编译缓存与正在运行的开发进程不同步时的常见现象。以下命令只会停止本项目的 Node 进程并删除可自动重新生成的 `.next` 缓存，不会删除文章、图片、代码或 `node_modules`。

将以下整段粘贴到 PowerShell 执行：

```powershell
$project = 'D:\project\code2\temp_pastemd\paper\tailwind-nextjs-starter-blog'
$nodePids = Get-CimInstance Win32_Process -Filter "Name='node.exe'" |
  Where-Object { $_.CommandLine -match [regex]::Escape($project) } |
  Select-Object -ExpandProperty ProcessId

if ($nodePids) {
  Stop-Process -Id $nodePids -Force
}

Remove-Item -LiteralPath "$project\.next" -Recurse -Force -ErrorAction SilentlyContinue
Set-Location $project
corepack yarn start
```

等待看到 `Ready` 后刷新浏览器；建议按 `Ctrl + F5` 强制刷新一次。

## 三、构建验证（提交或部署前）

先停止开发服务，再执行：

```powershell
Set-Location 'D:\project\code2\temp_pastemd\paper\tailwind-nextjs-starter-blog'
corepack yarn build
```

构建成功后，再按“正常重启”启动：

```powershell
corepack yarn start
```

## 四、确认是否已运行

另开一个 PowerShell 窗口执行：

```powershell
Invoke-WebRequest 'http://localhost:3000' -UseBasicParsing | Select-Object StatusCode
```

返回 `200` 表示本地网站可访问。

## 五、查看后台启动日志（仅适用于后台启动）

```powershell
$project = 'D:\project\code2\temp_pastemd\paper\tailwind-nextjs-starter-blog'
Get-Content "$project\dev-server.log" -Tail 80
Get-Content "$project\dev-server-error.log" -Tail 80 -ErrorAction SilentlyContinue
```

## 六、咨询辅导入口开关

咨询辅导当前已统一隐藏。要恢复所有咨询辅导入口，只修改下列文件的一行：

`data\siteFeatures.ts`

```ts
export const consultingEnabled = true
```

修改后按“二、缓存报错或修改未生效时”重启项目即可。改回 `false` 则再次隐藏入口。
