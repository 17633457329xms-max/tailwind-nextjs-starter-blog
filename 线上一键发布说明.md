# 线上一键发布说明

脚本位置：`scripts\发布到线上.ps1`。

它会按顺序完成：

1. 本地 TypeScript 类型检查；
2. 暂存业务代码（自动排除 `reports/content-quality-audit.json`、`tsconfig.tsbuildinfo`）；
3. 要求填写中文提交说明，创建提交并推送 GitHub；
4. 通过 SSH 登录服务器 `admin@120.55.82.186`；
5. 服务器拉取 `main`、安装锁定依赖、构建、PM2 重启并检查本机 3000 端口；
6. 本机请求 `https://lunwenhub.com/`，确认线上返回 HTTP 200。

## 每次发布如何执行

1. 先在本地完成代码修改并保存。
2. 打开 PowerShell，执行：

```powershell
Set-Location 'D:\project\code2\temp_pastemd\paper\tailwind-nextjs-starter-blog'
.\scripts\发布到线上.ps1
```

3. 出现“请输入本次中文提交说明”时，输入一句中文，例如：`完善金融学论文选题文章`，按回车。
4. 如首次 SSH 连接提示确认服务器指纹，输入 `yes`；如未配置 SSH 密钥，再输入服务器 `admin` 用户密码。
5. 看到“发布成功”即完成。浏览器可按 `Ctrl + F5` 强制刷新确认。

## 常见情况

- **只修改了环境变量**：不要运行此脚本。应 SSH 登录服务器，单独修改 `/home/admin/paper-site/.env.production`，然后执行 `pm2 reload paper-site --update-env`。环境变量和私钥不应提交 GitHub。
- **服务器提示本地改动阻止拉取**：脚本会安全停止，不会覆盖。登录服务器执行 `cd /home/admin/paper-site && git status`，将需要保留的内容另存后再处理。
- **想跳过本地类型检查**：仅在已确认本地检查通过时执行：

```powershell
.\scripts\发布到线上.ps1 -跳过类型检查
```

- **线上构建需要时间**：服务器配置较低时，构建约需数分钟；脚本运行期间请不要关闭 PowerShell。
