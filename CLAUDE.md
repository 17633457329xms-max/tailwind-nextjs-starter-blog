# 项目规则

本项目的协作规则统一维护在 @AGENTS.md，请完整阅读并遵守。

其中最关键的一条，对每次代码修改都强制生效：**修改完代码必须自动测试（prettier → typecheck → build → 起服务实际访问确认），确保正常运行、确认无误后，才可以提交并推送到 GitHub 远程，提交信息需符合 Conventional Commits 规范（中文）。** 测试失败或未验证完整，不允许提交。
